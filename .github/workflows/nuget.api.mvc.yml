name: Publish ProjectA NuGet Package

on:
  push:
    branches:
      - main
      - develop
    paths:
      - '**/ZEA.Api.MVC/**'
  workflow_dispatch:

jobs:
  publish-package:
    uses: ./.github/workflows/template.nuget.publish.yml
    with:
      projectName: 'ZEA.Api.MVC'
      isPreRelease: ${{ github.ref != 'refs/heads/main' }}
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}