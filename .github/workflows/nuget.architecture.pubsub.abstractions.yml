name: Publish ZEA.Architecture.PubSub.Abstractions

on:
  push:
    branches:
      - main
      - develop
    paths:
      - '**/ZEA.Architecture.PubSub.Abstractions/**'
  workflow_dispatch:

jobs:
  publish-package:
    uses: ./.github/workflows/template.nuget.publish.yml
    with:
      projectName: 'ZEA.Architecture.PubSub.Abstractions'
      isPreRelease: ${{ github.ref != 'refs/heads/main' }}
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}