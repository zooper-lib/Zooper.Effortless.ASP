name: Publish ZEA.Serialization.NewtonsoftJson

on:
  push:
    branches:
      - main
      - develop
    paths:
      - '**/ZEA.Serialization.NewtonsoftJson/**'
  workflow_dispatch:

jobs:
  publish-package:
    uses: ./.github/workflows/template.nuget.publish.yml
    with:
      projectName: 'ZEA.Serialization.NewtonsoftJson'
      isPreRelease: ${{ github.ref != 'refs/heads/main' }}
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}