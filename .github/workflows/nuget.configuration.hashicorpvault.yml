name: Publish ZEA.Configuration.HashicorpVault

on:
  push:
    branches:
      - main
      - develop
    paths:
      - '**/ZEA.Configuration.HashicorpVault/**'
  workflow_dispatch:

jobs:
  publish-package:
    uses: ./.github/workflows/template.nuget.publish.yml
    with:
      projectName: 'ZEA.Configuration.HashicorpVault'
      isPreRelease: ${{ github.ref != 'refs/heads/main' }}
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}