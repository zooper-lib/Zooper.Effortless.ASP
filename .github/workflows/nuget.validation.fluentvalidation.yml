name: Publish NuGet Package

on:
  push:
    branches:
      - main
      - develop
    paths:
      - '**/ZEA.Validation.FluentValidation/**'
  workflow_dispatch:

jobs:
  publish-package:
    uses: ./.github/workflows/template.nuget.publish.yml
    with:
      projectName: 'ZEA.Validation.FluentValidation'
      isPreRelease: ${{ github.ref != 'refs/heads/main' }}
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}